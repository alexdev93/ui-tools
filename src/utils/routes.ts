export const routeConstants = {
  activateAccount: "activate",
  resetPassword: "reset",
  // dashboard: "/#/dashboard",
  login: "login",
};

export const publicRoutes = [
  `#/${routeConstants.activateAccount}`,
  `#/${routeConstants.resetPassword}`,
  `#/${routeConstants.login}`,
];
